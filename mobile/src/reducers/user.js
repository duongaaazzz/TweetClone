const initialState = {
    token: null,
    insAuthenticated: false,
    info: null
}

export default (state = initialState, action) => {
    switch (action.type){
        default:
            return state;
    }
}